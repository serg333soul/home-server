# 📘 DevOps Troubleshooting Cheat Sheet
> Особиста база знань для діагностики та роботи з Linux/Docker.

## 📂 1. Розвідка (Дивимось логи)

### `tail -f [файл]`
* **Що робить:** Показує кінець файлу в реальному часі (живий моніторинг).
* **Приклад:** `tail -f /var/log/syslog`
* **Коли:** Коли ви запустили процес і чекаєте помилку прямо зараз.

### `cat [файл]`
* **Що робить:** Виводить весь вміст файлу на екран.
* **Приклад:** `cat /var/log/file_transfer.log`
* **Коли:** Щоб прочитати старі логи або короткі конфіги.

### `grep "[текст]" [де_шукати]`
* **Що робить:** Фільтр. Шукає слово у тексті.
* **Приклад:** `ls -R /var/lib/docker | grep MobileUploads`
* **Коли:** Коли треба знайти голку в копиці сіна (файл, папку або помилку в лозі).

---

## 🔍 2. Файлова система (Де мої дані?)

### `ls -l [шлях]`
* **Що робить:** Детальний список файлів (права, власник, розмір, дата).
* **Приклад:** `ls -l /mnt/ssd_storage/`
* **Коли:** Щоб перевірити, чи створився файл, і хто його власник (root чи www-data).

### `ls -R [шлях]`
* **Що робить:** Рекурсивний список (показує всі вкладені папки).
* **Приклад:** `ls -R /var/lib/docker/volumes/`
* **Коли:** Коли файл десь глибоко, але ви не знаєте де.

### `head -n 10 [файл]`
* **Що робить:** Показує перші 10 рядків файлу.
* **Приклад:** `head -n 10 script.sh`
* **Коли:** Щоб швидко глянути "шапку" скрипта (shebang, PATH) без відкриття редактора.

---

## 🛠️ 3. Діагностика скриптів

### `bash -x [скрипт]`
* **Що робить:** Debug Mode. Запускає скрипт, показуючи кожну команду і значення змінних.
* **Приклад:** `bash -x ./move_files.sh`
* **Коли:** Скрипт працює без помилок, але робить дурниці. Дозволяє побачити, що `$USER` це `-serg`, а не `serg`.

### `which [програма]`
* **Що робить:** Показує повний шлях до програми.
* **Приклад:** `which docker` -> `/usr/bin/docker`
* **Коли:** Cron каже `command not found`.

### `sed`
* **Що робить:** Редактор потоку (зміна тексту на льоту).
* **Приклад:** `sed 's/^[[:space:]-]*//'` (видаляє пробіли і дефіси на початку).
* **Коли:** Треба почистити "сміття" у змінних.

---

## 🐳 4. Docker

### `docker exec -u [user] [container] [command]`
* **Що робить:** Виконує команду всередині контейнера.
* **Приклад:** `docker exec -u 33 nextcloud-app-1 php occ files:scan --all`
* **Коли:** Треба змусити Nextcloud пересканувати файли або скинути пароль.

---

## ☢️ 5. Екстрені заходи

### `cat << 'EOF' > [файл]`
* **Що робить:** Повністю перезаписує файл текстом, вставленим у термінал.
* **Коли:** Коли редактори (nano) глючать або треба гарантовано оновити файл на сервері.

### `git reset --soft HEAD~1`
* **Що робить:** Відміняє останній коміт, але залишає файли.
* **Коли:** Коли випадково закоммітили щось зайве (великі файли) і треба переробити.

---

## 🧠 Алгоритм пошуку помилки
1. **Локалізація:** `tail -f logfile`
2. **Реальність:** `ls -l filename`
3. **Оточення:** `which command`
4. **Логіка:** `bash -x script.sh`
